id: "shopping-cart"
title: "Shopping Cart"
icon: "🛒"
vars:
  list_name: "cart"
  item_singular: "item"
  item_plural: "items"
  item_examples:
    - "coffee"
    - "milk"
    - "bread"
    - "eggs"
    - "cheese"
  record_keys:
    - "name"
    - "quantity"
    - "price"
  attr_key_1: "name"
  attr_key_2: "price"
  id_var: "product_id"
  filename: "cart.json"
  filetype: "json"
  user_role: "customer"
  status_var: "in_cart"
  action_verb: "buy"
  entity_name: "Product"
contexts:
  list-create-empty: "Start with an empty shopping cart for the user."
  list-create-values: "Initialize cart with previously saved items."
  tuple-create: "Store immutable product info like (sku, category)."
  list-append-dynamic: "Add an item the customer wants to buy."
  list-extend-method: "Add multiple items from a saved cart."
  list-insert-dynamic: "Insert item at a specific position."
  list-in-check: "Check if an item is already in the cart before adding."
  list-count: "Count how many of this item are in the cart."
  list-index: "Find the position of an item in the cart."
  dict-create-values: "Track item details like quantity and price."
  dict-create-empty: "Create an empty price lookup dictionary."
  dict-get-default: "Get item price, defaulting to 0."
  dict-set-item: "Update the quantity of an item."
  input-string: "Get the product name from the customer."
  print-string: "Display 'Item added!' message."
  print-variable: "Show the item's price."
  for-loop-list: "Display all items in the cart."
  for-loop-string: "Display each character of product name."
  for-loop-range: "Process cart items by position."
  enumerate-basic: "Number items so the user can remove by number."
  enumerate-start: "Number cart items starting from 1."
  zip-basic: "Combine product names with prices."
  f-string-basic: "Format item display: 'Product - $X.XX'."
  f-string-expression: "Show total with calculated tax."
  string-format: "Format the receipt output."
  list-sorted: "Display items sorted by name."
  list-sort: "Manage the cart collection."
  list-reverse: "Show most expensive items first."
  sorted-reverse: "Get items sorted by price, highest first."
  list-comp-filter-dynamic: "Show only items above a certain price."
  list-comp-basic: "Extract just the product names."
  list-comp-conditional: "Get items that are on sale."
  if-else-basic: "Check if customer has enough credit."
  if-only: "Check if cart is empty."
  boolean-and: "Check if item is in stock AND affordable."
  boolean-or: "Check if item is on sale OR clearance."
  elif-grade: "Apply discount tier: gold, silver, bronze."
  elif-http-status: "Handle different checkout states."
  match-case-basic: "Handle different cart actions."
  try-except-basic: "Handle out of stock errors."
  raise-intro: "Raise error for invalid quantities."
  raise-exception: "Raise OutOfStock when item unavailable."
  except-specific: "Catch KeyError for missing products."
  except-multi: "Handle both stock and payment errors."
  try-else: "Run code only if checkout succeeded."
  finally-basic: "Always release cart lock after checkout."
  try-except-finally: "Complete checkout with cleanup."
  reraise: "Re-raise after logging checkout error."
  define-function: "Create a function to calculate cart total."
  function-return: "Return the subtotal amount."
  return-none: "Return None when item is removed."
  default-param-simple: "Add item with default quantity 1."
  default-param-intro: "Define optional discount parameter."
  default-param-multiple: "Multiple defaults for item properties."
  context-intro: "Save the cart to resume shopping later."
  file-open-write: "Save cart to a file."
  file-read-lines: "Load saved cart data."
  pathlib-intro: "Use Path for cart file management."
  pathlib-check-exists: "Check if saved cart exists."
  pathlib-create-path: "Create path to carts directory."
  pathlib-join-paths: "Join paths for backup carts."
  main-guard-basic: "Only run checkout when executed directly."
  import-from: "Import pricing utilities."
  import-module: "Import the cart module."
  active-users-integrated: "Find items with their position in the cart."
  api-response-model-integrated: "Define a data model for Product API responses."
  apply-coupon: "Apply a discount to items in the cart."
  boolean-all-check: "Check if all items in cart meet a condition."
  conditional-dict-validation: "Validate item data before processing."
  conditional-early-return: "Guard against invalid item data early."
  conditional-list-any: "Check if any item meets the criteria."
  config-loader-integrated: "Safely load item configuration from JSON."
  dict-from-parallel-lists: "Combine item keys and values into a dictionary."
  discount-tier-elif: "Categorize items by tier or level."
  error-code-extractor-integrated: "Extract error codes from item operation results."
  error-handling-conversion: "Handle type conversion errors for item."
  error-handling-dict-access: "Safely access item dictionary keys."
  error-handling-file-read: "Handle errors when loading item data."
  export-orders-csv: "Export cart to a CSV file."
  file-read-to-list: "Load cart from a file into a list."
  file-write-from-list: "Save cart to a file."
  filter-list-of-dicts: "Filter cart by in_cart value."
  filter-orders-fn: "Create a function to filter cart."
  for-else: "Handle the case when no item is found."
  fstring-from-dict: "Format item details from a dictionary."
  health-check-dataclass-integrated: "Create a health check status for item services."
  high-value-orders-comp: "Find high-value items using comprehension."
  import-json-parse: "Parse item data from JSON format."
  input-to-int: "Convert user input to item quantity."
  inventory-dict-comp: "Build a item lookup using dict comprehension."
  inventory-item-class: "Define a Product class."
  inventory-reorder-loop: "Loop through cart and flag items for reorder."
  list-dedup-preserve-order: "Remove duplicate items while keeping order."
  list-to-dict-conversion: "Convert cart to a lookup dictionary."
  loop-dict-accumulate: "Sum values from item data."
  loop-nested-flatten: "Flatten nested cart into a single list."
  number-round-format: "Round and format item values."
  number-stats-from-list: "Calculate statistics from cart."
  order-status-ternary: "Set item status label in one line."
  order-total-method: "Add a method to calculate item total."
  orders-parse-to-records: "Parse item lines into records."
  orders-total-accumulator: "Accumulate totals across cart."
  parse-events-log: "Parse item event log entries."
  parse-quantity-safe: "Safely parse item quantity."
  process-orders-with-guard: "Process cart with cleanup on errors."
  product-tags-to-set: "Get unique item tags as a set."
  retry-decorator-integrated: "Retry failed item operations automatically."
  sort-dict-by-value: "Sort items by their values."
  sorted-key-lambda: "Sort cart using a custom key."
  string-join-transformed: "Join items into a formatted string."
  string-split-filter: "Split and filter item text."
  summarize-customers: "Count active items in cart."
  typehint-callable: "Add type hints to item processing function."
  validate-order-status: "Validate item status before changes."

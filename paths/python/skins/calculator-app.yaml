# paths/python/skins/calculator-app.yaml

id: calculator-app
title: "Calculator App"
icon: "🧮"
blueprints: [calculator-app]

vars:
  list_name: history
  item_singular: calculation
  item_plural: calculations
  item_examples:
    - "5 + 3"
    - "10 * 2"
    - "100 / 4"
    - "2 ** 8"
    - "15 % 4"
  record_keys: ["expression", "result", "timestamp"]
  attr_key_1: expression
  attr_key_2: result
  id_var: calc_id
  filename: history.json
  filetype: json
  user_role: user
  status_var: computed
  action_verb: calculate
  entity_name: Calculation

dataPack:
  list_sample: [1, 2, 3, 4, 5]
  dict_sample:
    expression: "5 + 3"
    result: 8
    timestamp: "2026-01-09"
  records_sample:
    - { expression: "10 + 5", result: 15 }
    - { expression: "20 / 4", result: 5 }
    - { expression: "3 * 7", result: 21 }
  string_samples:
    - "Result: "
    - "Enter a number: "
    - "Invalid input!"
    - "Calculation complete"

contexts:
  # Variables & Input
  assign-variable: "Store the user's input number."
  assign-number: "Store a numeric constant."
  multiple-assignment: "Store multiple values at once."
  input-string: "Get the expression from the user."
  input-number: "Get a numeric value from the user."
  print-variable: "Display the result to the user."

  # Arithmetic Operations
  operator-addition-fill: "Add two numbers together."
  int-addition-dynamic: "Perform addition with variables."
  int-multiply-dynamic: "Multiply two numbers."
  operator-floor-division-fill: "Divide and get the integer result."
  int-floor-div-fill: "Floor division returns an integer."
  int-power-fill: "Calculate powers (exponentiation)."
  modulo-dynamic: "Get the remainder after division."
  float-round-fill: "Round the result to a specified precision."
  float-abs-fill: "Get the absolute value of a number."
  float-predict-round: "Predict the result of rounding."
  float-predict-division: "Predict the result of division."
  operator-precedence-dynamic: "Apply correct order of operations."
  operator-parentheses-dynamic: "Use parentheses to control evaluation order."
  operator-expression-predict-dynamic: "Predict the result of an expression."

  # Type Conversion
  convert-int-fill: "Convert string input to a number."
  convert-str-fill: "Convert the result back to string for display."
  convert-predict-int: "Predict integer conversion result."
  convert-predict-bool: "Predict boolean conversion result."
  conversion-write-dynamic: "Write code to convert types."
  conversion-predict-dynamic: "Predict the result of type conversion."

  # Output
  f-string-basic: "Format the result nicely."
  print-fstring: "Print the formatted calculation."
  f-string-expression: "Show calculated value in output."

  # Boolean Logic
  bool-and-fill: "Check if both operands are valid."
  bool-or-fill: "Check if either operation is allowed."
  bool-predict-and: "Predict AND operation result."
  bool-predict-not: "Predict NOT operation result."
  bool-expression-dynamic: "Build a boolean expression."
  bool-logic-write-dynamic: "Write boolean logic."
  bool-expr-dynamic: "Create boolean expressions."
  bool-compound-dynamic: "Combine multiple boolean conditions."

  # Comparisons
  comparison-is-fill: "Check if result is None."
  identity-is-none: "Check if input is None."
  identity-is-not-none: "Verify input is not None."
  identity-vs-equality: "Understand == vs is."
  comparison-chaining: "Chain comparisons for range checks."
  comparison-predict-chain: "Predict chained comparison result."
  comparison-result-dynamic: "Write comparison expressions."

  # Truthiness
  truthiness-intro: "Understand truthy and falsy values."
  truthiness-empty-list: "Check if history is empty."
  truthiness-predict-empty: "Predict truthiness of empty values."
  truthiness-predict-zero: "Predict truthiness of zero."
  pythonic-if-empty-check: "Pythonic way to check empty input."

  # Validation & Error Handling
  if-else-basic: "Check if input is a valid number."
  if-only: "Check for division by zero."
  ternary-basic: "Return result or error in one line."
  try-except-basic: "Handle invalid numeric input."
  except-specific: "Catch ValueError for bad input."
  valueerror-catch: "Handle conversion errors."

  # Functions
  define-function: "Create a function to perform calculations."
  function-return: "Return the calculation result."
  default-param-simple: "Add precision parameter with default."
  elif-grade: "Handle different operations (+, -, *, /)."
  match-case-basic: "Match on the operation type."
  elif-http-status: "Handle different calculator modes."

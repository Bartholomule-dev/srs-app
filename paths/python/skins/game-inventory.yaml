id: "game-inventory"
title: "Game Inventory"
icon: "🎮"
vars:
  list_name: "inventory"
  item_singular: "item"
  item_plural: "items"
  item_examples:
    - "health potion"
    - "iron sword"
    - "magic ring"
    - "leather armor"
    - "gold coins"
  record_keys:
    - "name"
    - "quantity"
    - "weight"
  attr_key_1: "name"
  attr_key_2: "rarity"
  id_var: "item_id"
  filename: "inventory.json"
  filetype: "json"
  user_role: "player"
  status_var: "equipped"
  action_verb: "equip"
  entity_name: "Item"
contexts:
  list-create-empty: "Create the player's empty inventory."
  list-create-values: "Initialize inventory with starter items."
  tuple-create: "Store immutable item info like (type, rarity)."
  list-append-dynamic: "Pick up an item and add it to inventory."
  list-extend-method: "Loot multiple items from a chest."
  list-insert-dynamic: "Insert item at a specific slot."
  list-in-check: "Check if the player already has this item."
  list-count: "Count how many potions you have."
  list-index: "Find the position of the sword."
  dict-create-values: "Track item properties like quantity and weight."
  dict-create-empty: "Create an empty equipment slots dictionary."
  dict-get-default: "Get item damage, defaulting to 0."
  dict-set-item: "Equip an item to a slot."
  input-string: "Get the item name from player."
  print-string: "Display 'Item equipped!' message."
  print-variable: "Show the item's stats."
  for-loop-list: "Display all items in the player's inventory."
  for-loop-string: "Display each character of item name."
  for-loop-range: "Process inventory slots by index."
  enumerate-basic: "Number items for the use/drop menu."
  enumerate-start: "Number items starting from 1."
  zip-basic: "Combine item names with damage values."
  f-string-basic: "Format 'Item: Name (Damage: X)'."
  f-string-expression: "Show total weight with encumbrance."
  string-format: "Format inventory display."
  list-sorted: "Display items sorted by name."
  list-sort: "Sort inventory by item value."
  list-reverse: "Show highest value items first."
  sorted-reverse: "Get items sorted by damage."
  list-comp-filter-dynamic: "Show only items that can be equipped."
  list-comp-basic: "Extract just the item names."
  list-comp-conditional: "Get all rare or better items."
  if-else-basic: "Check if player can carry more items."
  if-only: "Check if inventory is empty."
  boolean-and: "Check if item is rare AND unequipped."
  boolean-or: "Check if item is weapon OR armor."
  elif-grade: "Categorize rarity: common, rare, legendary."
  elif-http-status: "Handle different game states."
  match-case-basic: "Handle inventory commands."
  try-except-basic: "Handle invalid item operations."
  raise-intro: "Raise error for overencumbered state."
  raise-exception: "Raise ItemNotFound error."
  except-specific: "Catch KeyError for missing slots."
  except-multi: "Handle both slot and weight errors."
  try-else: "Run code only if equip succeeded."
  finally-basic: "Always update inventory weight."
  try-except-finally: "Complete item handling with cleanup."
  reraise: "Re-raise after logging game error."
  define-function: "Create a function to calculate total weight."
  function-return: "Return the encumbrance level."
  return-none: "Return None when item is discarded."
  default-param-simple: "Add item with default quantity 1."
  default-param-intro: "Define optional rarity parameter."
  default-param-multiple: "Multiple defaults for item stats."
  context-intro: "Save the game state to continue later."
  file-open-write: "Write save game to disk."
  file-read-lines: "Load saved game data."
  pathlib-intro: "Use Path for save files."
  pathlib-check-exists: "Check if save file exists."
  pathlib-create-path: "Create path to saves directory."
  pathlib-join-paths: "Join paths for backup saves."
  main-guard-basic: "Only start game when run directly."
  import-from: "Import game utilities."
  import-module: "Import the game engine module."
  active-users-integrated: "Find items with their position in the inventory."
  api-response-model-integrated: "Define a data model for Item API responses."
  apply-coupon: "Apply a discount to items in the inventory."
  boolean-all-check: "Check if all items in inventory meet a condition."
  conditional-dict-validation: "Validate item data before processing."
  conditional-early-return: "Guard against invalid item data early."
  conditional-list-any: "Check if any item meets the criteria."
  config-loader-integrated: "Safely load item configuration from JSON."
  dict-from-parallel-lists: "Combine item keys and values into a dictionary."
  discount-tier-elif: "Categorize items by tier or level."
  error-code-extractor-integrated: "Extract error codes from item operation results."
  error-handling-conversion: "Handle type conversion errors for item."
  error-handling-dict-access: "Safely access item dictionary keys."
  error-handling-file-read: "Handle errors when loading item data."
  export-orders-csv: "Export inventory to a CSV file."
  file-read-to-list: "Load inventory from a file into a list."
  file-write-from-list: "Save inventory to a file."
  filter-list-of-dicts: "Filter inventory by equipped value."
  filter-orders-fn: "Create a function to filter inventory."
  for-else: "Handle the case when no item is found."
  fstring-from-dict: "Format item details from a dictionary."
  health-check-dataclass-integrated: "Create a health check status for item services."
  high-value-orders-comp: "Find high-value items using comprehension."
  import-json-parse: "Parse item data from JSON format."
  input-to-int: "Convert user input to item quantity."
  inventory-dict-comp: "Build a item lookup using dict comprehension."
  inventory-item-class: "Define a Item class."
  inventory-reorder-loop: "Loop through inventory and flag items for reorder."
  list-dedup-preserve-order: "Remove duplicate items while keeping order."
  list-to-dict-conversion: "Convert inventory to a lookup dictionary."
  loop-dict-accumulate: "Sum values from item data."
  loop-nested-flatten: "Flatten nested inventory into a single list."
  number-round-format: "Round and format item values."
  number-stats-from-list: "Calculate statistics from inventory."
  order-status-ternary: "Set item status label in one line."
  order-total-method: "Add a method to calculate item total."
  orders-parse-to-records: "Parse item lines into records."
  orders-total-accumulator: "Accumulate totals across inventory."
  parse-events-log: "Parse item event log entries."
  parse-quantity-safe: "Safely parse item quantity."
  process-orders-with-guard: "Process inventory with cleanup on errors."
  product-tags-to-set: "Get unique item tags as a set."
  retry-decorator-integrated: "Retry failed item operations automatically."
  sort-dict-by-value: "Sort items by their values."
  sorted-key-lambda: "Sort inventory using a custom key."
  string-join-transformed: "Join items into a formatted string."
  string-split-filter: "Split and filter item text."
  summarize-customers: "Count active items in inventory."
  typehint-callable: "Add type hints to item processing function."
  validate-order-status: "Validate item status before changes."

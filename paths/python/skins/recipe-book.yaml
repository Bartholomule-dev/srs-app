id: "recipe-book"
title: "Recipe Book"
icon: "📖"
vars:
  list_name: "recipes"
  item_singular: "recipe"
  item_plural: "recipes"
  item_examples:
    - "spaghetti carbonara"
    - "chicken curry"
    - "chocolate cake"
    - "caesar salad"
    - "beef stew"
  record_keys:
    - "name"
    - "servings"
    - "time_minutes"
  attr_key_1: "name"
  attr_key_2: "cuisine"
  id_var: "recipe_id"
  filename: "recipes.json"
  filetype: "json"
  user_role: "chef"
  status_var: "favorite"
  action_verb: "cook"
  entity_name: "Recipe"
contexts:
  list-create-empty: "Start your recipe book with an empty collection."
  list-create-values: "Initialize with your favorite recipes."
  tuple-create: "Store immutable recipe info like (cuisine, difficulty)."
  list-append-dynamic: "Add a new recipe to your collection."
  list-extend-method: "Import recipes from another cookbook."
  list-insert-dynamic: "Insert recipe at a specific position."
  list-in-check: "Check if you already have this recipe."
  list-count: "Count variations of a recipe."
  list-index: "Find the position of a recipe."
  dict-create-values: "Store recipe details like servings and prep time."
  dict-create-empty: "Create an empty ingredients dictionary."
  dict-get-default: "Get prep time, defaulting to 30 minutes."
  dict-set-item: "Update recipe's rating."
  input-string: "Get the recipe name from user."
  print-string: "Display 'Recipe saved!' message."
  print-variable: "Show the recipe name."
  for-loop-list: "List all your saved recipes."
  for-loop-string: "Display each character of recipe name."
  for-loop-range: "Process recipes by index."
  enumerate-basic: "Number recipes for quick selection."
  enumerate-start: "Number recipes starting from 1."
  zip-basic: "Combine recipe names with prep times."
  f-string-basic: "Format 'Recipe: Name (X min)'."
  f-string-expression: "Show total cooking time."
  string-format: "Format recipe card display."
  list-sorted: "Display recipes alphabetically."
  list-sort: "Sort recipes by prep time."
  list-reverse: "Show newest recipes first."
  sorted-reverse: "Get recipes sorted by rating."
  list-comp-filter-dynamic: "Find quick recipes under 30 minutes."
  list-comp-basic: "Extract just the recipe names."
  list-comp-conditional: "Get vegetarian recipes."
  if-else-basic: "Check if recipe is vegetarian."
  if-only: "Check if cookbook is empty."
  boolean-and: "Check if recipe is quick AND easy."
  boolean-or: "Check if recipe is breakfast OR brunch."
  elif-grade: "Categorize by difficulty: easy, medium, hard."
  elif-http-status: "Handle different recipe states."
  match-case-basic: "Handle cookbook commands."
  try-except-basic: "Handle missing cookbook file."
  raise-intro: "Raise error for invalid recipe format."
  raise-exception: "Raise RecipeNotFound error."
  except-specific: "Catch KeyError for missing recipes."
  except-multi: "Handle file and format errors."
  try-else: "Run code only if recipe saved."
  finally-basic: "Always close cookbook file."
  try-except-finally: "Complete recipe handling."
  reraise: "Re-raise after logging error."
  define-function: "Create a function to calculate total prep time."
  function-return: "Return the total cooking time."
  return-none: "Return None when recipe is deleted."
  default-param-simple: "Add recipe with default servings 4."
  default-param-intro: "Define optional cuisine parameter."
  default-param-multiple: "Multiple defaults for recipe properties."
  context-intro: "Save your recipe book to disk."
  file-open-write: "Write cookbook to disk."
  file-read-lines: "Load saved cookbook data."
  pathlib-intro: "Use Path for cookbook files."
  pathlib-check-exists: "Check if cookbook exists."
  pathlib-create-path: "Create path to cookbooks directory."
  pathlib-join-paths: "Join paths for backup cookbooks."
  main-guard-basic: "Only run cookbook app when executed directly."
  import-from: "Import recipe utilities."
  import-module: "Import the cookbook module."
  active-users-integrated: "Find recipes with their position in the recipes."
  api-response-model-integrated: "Define a data model for Recipe API responses."
  apply-coupon: "Apply a discount to recipes in the recipes."
  boolean-all-check: "Check if all recipes in recipes meet a condition."
  conditional-dict-validation: "Validate recipe data before processing."
  conditional-early-return: "Guard against invalid recipe data early."
  conditional-list-any: "Check if any recipe meets the criteria."
  config-loader-integrated: "Safely load recipe configuration from JSON."
  dict-from-parallel-lists: "Combine recipe keys and values into a dictionary."
  discount-tier-elif: "Categorize recipes by tier or level."
  error-code-extractor-integrated: "Extract error codes from recipe operation results."
  error-handling-conversion: "Handle type conversion errors for recipe."
  error-handling-dict-access: "Safely access recipe dictionary keys."
  error-handling-file-read: "Handle errors when loading recipe data."
  export-orders-csv: "Export recipes to a CSV file."
  file-read-to-list: "Load recipes from a file into a list."
  file-write-from-list: "Save recipes to a file."
  filter-list-of-dicts: "Filter recipes by favorite value."
  filter-orders-fn: "Create a function to filter recipes."
  for-else: "Handle the case when no recipe is found."
  fstring-from-dict: "Format recipe details from a dictionary."
  health-check-dataclass-integrated: "Create a health check status for recipe services."
  high-value-orders-comp: "Find high-value recipes using comprehension."
  import-json-parse: "Parse recipe data from JSON format."
  input-to-int: "Convert user input to recipe quantity."
  inventory-dict-comp: "Build a recipe lookup using dict comprehension."
  inventory-item-class: "Define a Recipe class."
  inventory-reorder-loop: "Loop through recipes and flag items for reorder."
  list-dedup-preserve-order: "Remove duplicate recipes while keeping order."
  list-to-dict-conversion: "Convert recipes to a lookup dictionary."
  loop-dict-accumulate: "Sum values from recipe data."
  loop-nested-flatten: "Flatten nested recipes into a single list."
  number-round-format: "Round and format recipe values."
  number-stats-from-list: "Calculate statistics from recipes."
  order-status-ternary: "Set recipe status label in one line."
  order-total-method: "Add a method to calculate recipe total."
  orders-parse-to-records: "Parse recipe lines into records."
  orders-total-accumulator: "Accumulate totals across recipes."
  parse-events-log: "Parse recipe event log entries."
  parse-quantity-safe: "Safely parse recipe quantity."
  process-orders-with-guard: "Process recipes with cleanup on errors."
  product-tags-to-set: "Get unique recipe tags as a set."
  retry-decorator-integrated: "Retry failed recipe operations automatically."
  sort-dict-by-value: "Sort recipes by their values."
  sorted-key-lambda: "Sort recipes using a custom key."
  string-join-transformed: "Join recipes into a formatted string."
  string-split-filter: "Split and filter recipe text."
  summarize-customers: "Count active recipes in recipes."
  typehint-callable: "Add type hints to recipe processing function."
  validate-order-status: "Validate recipe status before changes."

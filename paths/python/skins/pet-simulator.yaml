id: "pet-simulator"
title: "Pet Simulator"
icon: "🐕"
vars:
  list_name: "pets"
  item_singular: "pet"
  item_plural: "pets"
  item_examples:
    - "Buddy"
    - "Max"
    - "Luna"
    - "Whiskers"
    - "Charlie"
  record_keys:
    - "name"
    - "species"
    - "age"
    - "health"
  attr_key_1: "name"
  attr_key_2: "health"
  id_var: "pet_id"
  filename: "pets.json"
  filetype: "json"
  user_role: "owner"
  status_var: "happy"
  action_verb: "feed"
  entity_name: "Pet"
dataPack:
  list_sample:
    - "dog"
    - "cat"
    - "bird"
    - "fish"
  dict_sample:
    name: "Buddy"
    species: "Dog"
    age: 3
    health: 100
  records_sample:
    - name: "Buddy"
      species: "Dog"
      health: 100
    - name: "Whiskers"
      species: "Cat"
      health: 95
    - name: "Tweety"
      species: "Bird"
      health: 80
  string_samples:
    - "Your pet is hungry!"
    - "Pet is happy!"
    - "Time for a walk!"
    - "Pet needs attention"
contexts:
  define-class: "Create a Pet class to represent your virtual pet."
  class-keyword-fill: "Use the class keyword to define Pet."
  init-method: "Initialize a pet with name and species."
  init-self-fill: "Set up pet attributes in __init__."
  self-parameter: "Reference the pet instance with self."
  instance-attribute: "Store the pet's health as an attribute."
  instance-attribute-predict: "Predict the pet's attribute value."
  self-attribute: "Access the pet's name via self."
  instance-method: "Add a method to feed the pet."
  method-call-predict: "Predict what pet.feed() returns."
  method-call-dynamic: "Call the pet's method dynamically."
  instance-method-write-dynamic: "Write a method to play with the pet."
  inheritance-basic: "Create a Dog class that inherits from Pet."
  inheritance-predict: "Predict what Dog inherits from Pet."
  inheritance-isinstance: "Check if buddy is an instance of Pet."
  inheritance-super-init: "Call Pet's __init__ from Dog."
  super-fill: "Use super() to call parent method."
  inheritance-issubclass: "Check if Dog is a subclass of Pet."
  inheritance-override-method: "Override the speak() method in Dog."
  inheritance-override-dynamic: "Write an overridden method."
  inheritance-predict-dynamic: "Predict inherited behavior."
  inheritance-super-method: "Extend parent's behavior in subclass."
  method-resolution-order: "Understand Pet → Dog method lookup."
  property-getter: "Add a health property with a getter."
  property-return: "Return the pet's health level."
  property-readonly: "Make species a read-only property."
  property-setter: "Add a setter to validate health changes."
  property-setter-validation: "Validate health is between 0-100."
  property-deleter: "Clean up when pet is released."
  classmethod-basic: "Add a class method to count all pets."
  classmethod-cls-parameter: "Use cls to access Pet class."
  classmethod-call: "Call the class method on Pet."
  classmethod-factory: "Create a Pet from a dictionary."
  api-response-model-integrated: "Parse pet data from API response."
  staticmethod-basic: "Add a static method for age calculation."
  staticmethod-vs-classmethod: "Understand when to use each."
  custom-exception: "Create a PetError for invalid operations."
  raise-exception: "Raise PetError when health is negative."
  try-except-basic: "Handle errors when feeding a pet."
  active-users-integrated: "Find pets with their position in the pets."
  apply-coupon: "Apply a discount to pets in the pets."
  boolean-all-check: "Check if all pets in pets meet a condition."
  conditional-dict-validation: "Validate pet data before processing."
  conditional-early-return: "Guard against invalid pet data early."
  conditional-list-any: "Check if any pet meets the criteria."
  config-loader-integrated: "Safely load pet configuration from JSON."
  dict-from-parallel-lists: "Combine pet keys and values into a dictionary."
  discount-tier-elif: "Categorize pets by tier or level."
  error-code-extractor-integrated: "Extract error codes from pet operation results."
  error-handling-conversion: "Handle type conversion errors for pet."
  error-handling-dict-access: "Safely access pet dictionary keys."
  error-handling-file-read: "Handle errors when loading pet data."
  export-orders-csv: "Export pets to a CSV file."
  file-read-to-list: "Load pets from a file into a list."
  file-write-from-list: "Save pets to a file."
  filter-list-of-dicts: "Filter pets by happy value."
  filter-orders-fn: "Create a function to filter pets."
  for-else: "Handle the case when no pet is found."
  fstring-from-dict: "Format pet details from a dictionary."
  health-check-dataclass-integrated: "Create a health check status for pet services."
  high-value-orders-comp: "Find high-value pets using comprehension."
  import-json-parse: "Parse pet data from JSON format."
  input-to-int: "Convert user input to pet quantity."
  inventory-dict-comp: "Build a pet lookup using dict comprehension."
  inventory-item-class: "For Pet, define a Pet class."
  inventory-reorder-loop: "Loop through pets and flag items for reorder."
  list-dedup-preserve-order: "Remove duplicate pets while keeping order."
  list-to-dict-conversion: "Convert pets to a lookup dictionary."
  loop-dict-accumulate: "Sum values from pet data."
  loop-nested-flatten: "Flatten nested pets into a single list."
  number-round-format: "Round and format pet values."
  number-stats-from-list: "Calculate statistics from pets."
  order-status-ternary: "Set pet status label in one line."
  order-total-method: "Add a method to calculate pet total."
  orders-parse-to-records: "Parse pet lines into records."
  orders-total-accumulator: "Accumulate totals across pets."
  parse-events-log: "Parse pet event log entries."
  parse-quantity-safe: "Safely parse pet quantity."
  process-orders-with-guard: "Process pets with cleanup on errors."
  product-tags-to-set: "Get unique pet tags as a set."
  retry-decorator-integrated: "Retry failed pet operations automatically."
  sort-dict-by-value: "Sort pets by their values."
  sorted-key-lambda: "Sort pets using a custom key."
  string-join-transformed: "Join pets into a formatted string."
  string-split-filter: "Split and filter pet text."
  summarize-customers: "Count active pets in pets."
  typehint-callable: "Add type hints to pet processing function."
  validate-order-status: "Validate pet status before changes."
